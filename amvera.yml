meta:
  environment: jvm
  toolchain:
    name: gradle
    version: "16"
build:
  artifacts:
    build/libs/*.jar: /
  args: spring-boot:repackage
run:
  jarName: TMK_website-1.0-SNAPSHOT.jar
  persistenceMount: /data
  containerPort: "8080"
  servicePort: "8080"
